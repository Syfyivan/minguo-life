# 民国人生文字版运行时规格

版本：`v0.5.2`

本文件只描述代码实际执行的规则。

## 一年内的处理顺序

1. 玩家安排本年行动
2. 结算能力、资源、主体、联系人和信息渠道
3. 生成一个具体年度生活场景；选择回响优先于随机日常
4. 处理当年时代事件，分别记录“知道名称”或“只承受冲击”
5. 检查其他主体的死亡、失联和确认
6. 呈现关键抉择，玩家只决定自己能执行的行动
7. 写入曲线、事实与行动反馈
8. 结算年龄带来的身体变化和主人公死亡窗口
9. 若主人公死亡，写入发生与确认事实并生成完整生平；否则进入下一年

1949 年走完第 7 步后正常进入 1950 年，不触发终局。

界面必须把第 3 步的“个人年度日常”和第 4 步的“时代近况”分成两张卡。1950 年以后，时代近况按大陆、香港、台湾、海外／流动的实际落点筛选；有报刊、书籍或可靠口风时显示事件名称和背景，否则只显示角色当时能感受到的房租、物价、工作、交通或制度影响。史实条目保留来源链接，个人未知不能被全知旁白强行补齐。

## 人生分层

运行时分别记录：

```text
birth family
→ livelihood
→ war turn
→ postwar settlement
→ post-1949 choice
→ later life
→ death
```

对应字段为 `familyKey / livelihoodKey / warTurnKey / postwarSettlementKey / post1949Choice / post1949 / life`。路径切换不会修改角色身份。

## 1949 后状态

`post1949Choice` 只允许：

- `mainland`
- `hong-kong`
- `taiwan`
- `overseas`
- `in-motion`
- `unsettled`

`post1949` 至少可保存：

- `arrival`
- `place`
- `livelihood`
- `livelihoodLater`
- `companions`
- `leftBehind`
- `correspondence`
- `care`
- `legacy`
- `employment`

这些字段进入分章节终局，不得只保留一个地域标签。

### 战后谋生状态

`post1949.employment` 保存：`status / track / role / workplace / duties / terms / startedYear / attempts / yearsWorked / lastResult / lastResultYear / nextStep / history[]`。

状态链为：

```text
尚未安排／正在找门路
→ 已有按日短工／正在试工／已经留用
→ 完成试工并留用／继续短工争取固定工期／继续当前工作
→ 五十岁后继续旧业、转换工作或减少工时
```

求职不保证直接录用，但必须在当年产生可解释结果。短工要写明岗位与结算期限；试工要写明职责、期限与答复节点；落选要写明原因和下一条可执行路径。已经留用后，同一行动必须改写为续工，不得继续显示“找工作”。

## 年度生活

`annualNarratives[]` 从出生年起每年追加一条记录。符合 `requiresEchoes` 的场景优先触发；事件池用尽后使用家庭、路径或 1949 后地域节律。

短于完整小故事标准的作者事件会按家庭或路径加入现实场景框架。最终玩家所见文本必须包含具体人物或群体、生活地点、现实原因、当下取舍、结果和未解决后账，不得只写一句抽象气氛。

所有民国阶段普通场景默认以 1949 年为最晚适用年份。1950 年以后只允许抽取明确标注 `post1949Choices` 的地域场景、五十岁以后的年龄回响或对应地域生活节律；不得把战时点名、流亡屋檐、旧家账房等旧场景套到香港、台湾或海外新落点。

`v0.5.0` 旧存档如果已把 1950 年后的旧路径节律写入年度记录，导入时按存档中已确认的 `post1949Choice` 迁移为对应地域节律。迁移只校正不合时地的展示文本，不重算当年已生效的数值、主体状态或随机序列。

`v0.5.1` 及更早存档若只有宽泛 `livelihood` 而没有 `employment`，导入时标记为“正在找门路”，公开说明具体职业记录缺失；下一次谋生行动再按当前地域与既有经历生成岗位和当年结果，不倒填旧年份的虚假录用事实。

## 行动与抉择提示

- 精神显示精确消耗或恢复
- 能力和资源只显示定性上升方向与下降风险
- 说明会影响哪些具体人物、主体和信息渠道
- 不显示内部精确属性点
- 锁定理由必须含需要值、当前值或缺少的具体信息渠道，并说明可改善方向
- 与当前家庭、性别或 1949 去向无关的内容可隐藏
- 路径条件不符但有理解价值时，要说明当前路径与需要路径
- 谋生行动额外显示具体目标岗位；结算后显示当年结果和下一步，不用属性增加代替就业事实

## 死亡规则

`life.status` 只允许 `alive` 或 `dead`；过程事实通过字段与事实账本体现：

```text
alive
→ dangerSince
→ deathOccurredYear
→ deathConfirmedYear
→ over
```

死亡窗口由固定种子、当前年龄和健康共同确定；健康归零可以提前触发死亡，最大年龄提供硬上限。死亡地点优先取 1949 后的实际落脚处，死亡原因根据健康、士兵伤病、迁徙劳损或高龄确定。

流动或落点未定状态下，死亡确认可以晚于死亡发生一年；其他状态可以同年确认，但仍必须写成两条独立事实。

## 完整生平

`buildLifeChapters()` 固定输出七章：

1. 出生与成长
2. 成年谋生
3. 战争转折
4. 战后重接
5. 1949 与后半生
6. 中晚年
7. 死亡与确认

`buildEndingFacts()` 另外回收关系、联系人、知情边界、长期选择和未知事实。主人公未死亡时允许预览章节，但不得生成正式终局。

## 存档

存档格式：`minguo-life-save`，schema `2`。

导入时：

- 补齐联系人、年度叙事、1949 后状态和死亡状态
- 年份上限为出生年加最大年龄，不再截断到 1949
- v0.4 已在 1949 结束且没有主人公死亡事实的存档会恢复为进行中，并从 1950 年继续
- 旧 `hktw` 因没有可反推信息，保守迁移为 `hong-kong`

## 完成条件

- 三个家庭、十一条路径均有固定种子代表态
- 六种 1949 后去向均可进入并继续到死亡
- 所有代表态包含 1949 里程碑、主人公死亡发生和死亡确认
- 所有代表态都有七章完整生平
- 所有推进年度都有一条具体生活叙事
- 所有 66 个行动与 143 个关键选项存在兼容可达人生
- 六种 1949 后去向都有具体岗位结果；求职、短工、试工、留用与续工状态可以连续推进并进入终局
- 主体、信息、联系人和家庭生命周期证据成立
- 存档可导出、导入并迁移上一版本
- 相同种子与选择得到完全一致的事实账本
- 终局文本无成功、失败、安稳、挣扎等价值排名
