# 文字版运行时规格

本文件只描述代码已经执行的规则，不承担世界观扩写。

## 一年内的处理顺序

1. 玩家安排本年行动
2. 引擎结算能力、资源、主体、联系人和信息渠道
3. 引擎生成一条当年普通生活场景或路线节律
4. 引擎处理当年时代事件，并分别记录“知道名称”或“只承受冲击”
5. 引擎检查主体死亡、失联和确认状态
6. 引擎呈现当年关键抉择；其他主体按自身条件回应
7. 所有抉择解决后写入曲线采样点与跨年事实
8. 年份推进并恢复精神；1949 年完成事实终局

## 状态边界

### 身份

`identity` 只由出生家庭、姓名、性别、出生年份和地点构成。路径切换不得修改身份。

### 路径

`routeKey` 是当前主要生活路径；`routeHistory` 保存每次路径改变的年份、来源与前后路径。

### 主体

`subjects` 中每个主体有独立的 `status / health / agency / strength / note`。玩家行动可以改善或恶化条件，但不能直接替主体写出所有最终选择。

### 联系人

`contacts` 保存每个出生家庭的三名具名人物。`relation / status / history` 跨年保留；联系人能提供门路或互助，但不能被主角的统一关系数值替代。

### 事实

`facts[]` 是唯一终局事实来源。每条事实必须包含：

- 稳定 id
- 发生或确认年份
- 类别
- 事实文字
- 来源事件或规则

### 信息

`information.channels[]` 记录已经建立的渠道；`knownEvents[]` 与 `unknownImpacts[]` 必须分开。

### 随机

每局保存 `seed` 和 `randomState`。随机只允许影响同一边界内的具体回应，不能无提示地替玩家完成主要路径选择。

### 年度生活

`annualNarratives[]` 从出生年起每年追加一条记录。优先抽取尚未发生且符合家庭、路径和年龄的普通事件；事件池暂时用尽后，从对应家庭或路径的年度节律中确定性抽取。

### 存档

存档包含内容版本、身份、随机状态、行动、路径史、主体、联系人、年度叙事和事实账本。导入时为上一版本补充安全默认字段，不伪造旧年份中的人物互动。

## 浏览器接口

`window.__DEMO` 暴露：

- `G`：当前状态只读入口
- `startFamily(familyKey, gender, name, seed)`
- `advance()`
- `choose(optionId)`
- `autoPlan()`
- `exportGame()`
- `importGame(payload)`
- `saveSummary()`
- `inspectState()`
- `inspectCoverage(states)`
- `inspectWholeGameProgressBundle(states)`

## 完成条件

文字版纵向闭环只有在以下条件同时成立时才可以标记完成：

- 三个家庭都有可到达终局的代表态
- 九条路径都有至少一个固定种子代表态
- 所有代表态都包含 1949 事实
- 所有代表态都包含主体证据
- 所有代表态都包含知情或不知情证据
- 所有代表态都包含联系人和家庭生命周期证据
- 所有推进年度都有年度生活叙事
- 存档可以导出、导入并迁移上一版本
- 终局文本不出现成功、失败、安稳、挣扎等价值排名
- 相同种子与选择得到完全一致的事实账本
