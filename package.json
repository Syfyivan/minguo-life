{
  "name": "minguo-life-text-prototype",
  "version": "0.4.0",
  "private": true,
  "description": "民国人生完整一生内容验证版",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "regression": "node demo-regression.mjs",
    "verify": "npm test && npm run regression"
  }
}
